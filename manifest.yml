display_information:
  name: slack-channel-list
features:
  bot_user:
    display_name: slack-channel-list
    always_online: false
oauth_config:
  scopes:
    bot:
      - channels:read
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
